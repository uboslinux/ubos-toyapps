developer="http://indiecomputing.com/"
url="http://ubos.net/"
maintainer=${developer}
pkgname=$(basename $(pwd))
pkgver=0.2
pkgrel=1
pkgdesc="Best Guestbook Evar, in Java this time"
arch=('any')
makedepends=('maven')
license=('GPL')
options=('!strip')

prepare() {
    cd ${startdir}/maven
    sed -e "s/PKGBUILD_VERSION/${pkgver}/" pom.xml > pom-with-version.xml
    mvn clean install -f pom-with-version.xml
}

package() {
# Manifest
    mkdir -p ${pkgdir}/var/lib/ubos/manifests
    install -m0644 ${startdir}/ubos-manifest.json ${pkgdir}/var/lib/ubos/manifests/${pkgname}.json

# Icons
    mkdir -p ${pkgdir}/srv/http/_appicons/${pkgname}
    install -m644 ${startdir}/appicons/{72x72,144x144}.png $pkgdir/srv/http/_appicons/${pkgname}/
    install -m644 ${startdir}/appicons/license.txt $pkgdir/srv/http/_appicons/${pkgname}/

# Code
    mkdir -p ${pkgdir}/usr/share/${pkgname}/lib
    install -m755 ${startdir}/maven/target/${pkgname}-${pkgver}.war ${pkgdir}/usr/share/${pkgname}/lib/${pkgname}.war

    mkdir -p $pkgdir/usr/share/${pkgname}/tmpl
    install -m755 ${startdir}/tmpl/context.xml.tmpl ${pkgdir}/usr/share/${pkgname}/tmpl/
    install -m755 ${startdir}/tmpl/htaccess.tmpl    ${pkgdir}/usr/share/${pkgname}/tmpl/

    mkdir -p ${pkgdir}/usr/share/${pkgname}/sql
    install -m755 ${startdir}/sql/create.sql ${pkgdir}/usr/share/${pkgname}/sql/
}
